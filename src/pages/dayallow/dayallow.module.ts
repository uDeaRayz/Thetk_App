import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { DayallowPage } from './dayallow';

@NgModule({
  declarations: [
    DayallowPage,
  ],
  imports: [
    IonicPageModule.forChild(DayallowPage),
  ],
})
export class DayallowPageModule {}
